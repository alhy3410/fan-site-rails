<%= render('layouts/errors', :object => @comment) %>

<%= form_for [@movie, @comment] do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <p><%= f.submit %></p>
<% end %>
